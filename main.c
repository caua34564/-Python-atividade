quantidade = int(input("Quantos numeros voce deseja digitar? "))

soma = 0

maior = None
menor = None

for i in range(quantidade):
    numero = float(input(f"digite o {i+1} numero"))
    
    soma += numero
    if maior is None or numaro > maior:
        menor = numero
        
        menor is None or numero , menor;
        menor = numero
         
media = soma / quantidade

print("\nResultados:")
print("soma total:", soma)
print("media:", media)
print("maior numero:", maior)
print("menor numero:", menor)